<Window x:Class="Interfaz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LOGIN"
        Width="372"
        Height="265"
        Background="White"
        WindowStartupLocation="CenterScreen">

    <StackPanel Margin="20">
        <!-- Título -->
        <TextBlock Text="Iniciar Sesión" 
                   FontSize="22" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Foreground="#333" 
                   Margin="0,0,0,20"/>

        <!-- Grid para formulario -->
        <Grid HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Usuario -->
            <TextBlock Text="Usuario" Grid.Column="0" VerticalAlignment="Center" Foreground="#555" Margin="25,0,5,0"/>
            <TextBox x:Name="UsuarioTextBox" Grid.Row="0" Grid.Column="1" Width="200" Background="#F9F9F9" BorderBrush="#CCC"/>

            <!-- Contraseña -->
            <TextBlock Text="Contraseña" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Foreground="#555" Margin="25,2,199,0" Grid.ColumnSpan="2"/>
            <PasswordBox x:Name="ContrasenaBox" Grid.Row="1" Grid.Column="1" Width="200" Background="#F9F9F9" BorderBrush="#CCC"/>
        </Grid>

        <!-- Botones -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Iniciar sesión" Width="150" Background="#4CAF50" Foreground="White" BorderBrush="#4CAF50" Click="Button_Click"/>
            <Button Content="Olvidé contraseña" Width="150" Background="#2196F3" Foreground="White" BorderBrush="#2196F3" Margin="10,0,0,0" Click="Button_Click_1"/>
        </StackPanel>
    </StackPanel>
</Window>

